/*** @author borella*/package com.duff.utils.display {		import flash.display.MovieClip;	public class ResizeItem {				// ___________________________________________________________________ PUBLIC METHODS		/**		 * Aplica resize no mc proporcionalmente e de acordo com o tamanho do stage		 * @param 	MovieClip alvo		 * @param	stage		 */			    public static function resizeMc(_alvo:MovieClip, _$stage:*):void		{				var getImgW:Number = _alvo.width;				var getImgH:Number = _alvo.height;								var getImageVector:Number = getImgW / getImgH;				var getScreenVector:Number = _$stage.stageWidth / _$stage.stageHeight;						var getVector:String = (getImageVector >= getScreenVector) ? "horizontal" : "vertical";							if(getVector == "horizontal"){								_alvo.height = _$stage.stageHeight;					_alvo.width = getImgW * (_$stage.stageHeight / getImgH);					_alvo.y = 0;					_alvo.x = (_$stage.stageWidth - _alvo.width) / 2;				}else{					_alvo.width = _$stage.stageWidth;					_alvo.height = getImgH * (_$stage.stageWidth / getImgW);					_alvo.x = 0;					_alvo.y = (_$stage.stageHeight - _alvo.height) / 2;				}					}			}}